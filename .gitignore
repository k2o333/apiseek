# Secrets and runtime data — never commit
sites/*.env
sites/*.env.*
!sites/*.env.example
data/
__pycache__/
*.pyc
*.tmp.*
monitor.lock

# Dotfiles / dot-directories (e.g. .venv, .tmp, .pytest_cache)
.*
!.gitignore
